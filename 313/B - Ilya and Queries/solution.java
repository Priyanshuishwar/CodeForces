import java.util.*;public class Main{	public static void main(String[] args) {		Scanner sc = new Scanner(System.in);		String s = sc.next();		int dp[] = new int[s.length()];		dp[0] = 0;// 		dp[s.length()] = 0;		for(int i = 1; i < s.length(); i++){		    dp[i] = dp[i - 1];		    if(s.charAt(i) == s.charAt(i - 1)){		        dp[i]++;		    }		}		int m = sc.nextInt();		while(m -- > 0){		    int l = sc.nextInt() - 1;		    int r = sc.nextInt() - 1;		    int ans = 0;		    ans = dp[r] - dp[l];		    System.out.println(ans);		}	}}