import java.util.*; public class cff {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);         int n = sc.nextInt();        int arr[] = new int[n];        int maxVal = 0;         // Input and find max value        for (int i = 0; i < n; i++) {            arr[i] = sc.nextInt();            if (arr[i] > maxVal) {                maxVal = arr[i];            }        }             long freq[] = new long[maxVal + 1];         for (int i = 0; i < n; i++) {            freq[arr[i]]++;        }         // DP array        long dp[] = new long[maxVal + 1];         dp[0] = 0;        dp[1] = freq[1] * 1;         for (int i = 2; i <= maxVal; i++) {            dp[i] = Math.max(dp[i - 1], dp[i - 2] + i * freq[i]);        }         System.out.println(dp[maxVal]);    }}