import java.io.*;import java.util.*; public class Main {    public static void main(String[] args) throws IOException {        FastReader fr = new FastReader();        PrintWriter pw = new PrintWriter(new BufferedOutputStream(System.out));         int MAX = 100_000;        int[] freq = new int[MAX + 1];         int n = fr.nextInt();         for (int i = 0; i < n; i++) {            int x = fr.nextInt();            freq[x]++;        }         for (int i = 1; i <= MAX; i++) {            freq[i] += freq[i - 1];        }         int q = fr.nextInt();        while (q-- > 0) {            int m = fr.nextInt();             if (m >= MAX)                pw.println(freq[MAX]);            else                pw.println(freq[m]);        }         pw.flush();    }     static class FastReader {        BufferedReader br;        StringTokenizer st;         public FastReader() {            br = new BufferedReader(new InputStreamReader(System.in));        }         String next() throws IOException {            while (st == null || !st.hasMoreTokens()) {                String line = br.readLine();                if (line == null) return null;                st = new StringTokenizer(line);            }            return st.nextToken();        }         int nextInt() throws IOException {            return Integer.parseInt(next());        }    }}